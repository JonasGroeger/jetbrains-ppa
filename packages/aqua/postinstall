#!/bin/bash

set -e

cd /opt/

# Create folder
mkdir -p "aqua"

# Move downloaded .tar.gz
mv "aqua.tar.gz" "aqua"

cd "aqua"

# Extract the package
tar -xzf "aqua.tar.gz" --strip-components 1

# Remove the tar.gz package
rm -f "aqua.tar.gz"
