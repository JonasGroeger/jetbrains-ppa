#!/bin/bash

set -e

rm -f /opt/jetbrains-toolbox-app.tar.gz # Remove the empty file, if the download failed.
rm -rf /opt/jetbrains-toolbox-app
rm -rf ~/.local/share/JetBrains # It installs itself on first boot in this folder.

##DEBHELPER##
