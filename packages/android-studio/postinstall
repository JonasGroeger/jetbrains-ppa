#!/bin/bash

set -e

cd /opt/

# Create folder
mkdir -p "android-studio"

# Move downloaded .tar.gz
mv "android-studio.tar.gz" "android-studio"

cd "android-studio"

# Extract the package
tar -xzf "android-studio.tar.gz" --strip-components 1

# Remove the tar.gz package
rm -f "android-studio.tar.gz"
