#!/bin/bash

set -e

cd /opt/

# Create folder
mkdir -p "pycharm"

# Move downloaded .tar.gz
mv "pycharm.tar.gz" "pycharm"

cd "pycharm"

# Extract the package
tar -xzf "pycharm.tar.gz" --strip-components 1

# Remove the tar.gz package
rm -f "pycharm.tar.gz"
