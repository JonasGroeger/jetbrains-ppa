#!/bin/bash

set -e

cd /usr/share/fonts/truetype/

# Create folder
mkdir -p "jetbrains-mono"

# Move downloaded .zip
mv "JetBrainsMono.zip" "jetbrains-mono"

cd "jetbrains-mono"

# Extract the package
unzip -quj "JetBrainsMono.zip" fonts/ttf/*.ttf fonts/variable/*.ttf

# Update font cache
fc-cache -f -v

# Remove the zip package
rm -f "JetBrainsMono.zip"
