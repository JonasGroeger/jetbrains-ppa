#!/bin/bash

set -e

cd /opt/

# Create folder
mkdir -p "dottrace"

# Move downloaded .tar.gz
mv "JetBrains.dotTrace.tar.gz" "dottrace"

cd "dottrace"

# Extract the package
tar -xzf "JetBrains.dotTrace.tar.gz" --strip-components 1

# Remove the tar.gz package
rm -f "JetBrains.dotTrace.tar.gz"
